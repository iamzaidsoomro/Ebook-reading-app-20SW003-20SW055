class BookModel {
  String thumb, id, summary, author, title, type;
  BookModel(
      {required this.author,
      required this.id,
      required this.summary,
      required this.thumb,
      required this.title,
      required this.type});
}
